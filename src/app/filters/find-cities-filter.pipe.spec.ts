import { FindCitiesFilterPipe } from './find-cities-filter.pipe';

describe('FindCitiesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FindCitiesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
